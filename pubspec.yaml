name: exview_follower
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  http: ^0.12.0+2
  provider: ^3.1.0
  shared_preferences: ^0.5.3+4
  fluttertoast: ^3.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  cipher2:
    path: plugin/cipher2
    
flutter:
  uses-material-design: true

  assets:
    - skmm.png